<%= form_with(model: quize, local: true) do |form| %>
  <% if quize.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quize.errors.count, "error") %> prohibited this quize from being saved:</h2>

      <ul>
      <% quize.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :names %>
    <%= form.text_field :names %>
  </div>

  <div class="field">
    <%= form.label :quiz %>
    <%= form.text_field :quiz %>
  </div>

  <div class="field">
    <%= form.label :marks %>
    <%= form.text_field :marks %>
  </div>

  <div class="field">
  <%= form.hidden_field :course_id, :value => params[:course_id] if params[:course_id] %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
