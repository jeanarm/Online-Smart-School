<%= form_with(model: exam, local: true) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% exam.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :names %>
    <%= form.text_field :names %>
  </div>

  <div class="field">
    <%= form.label :mid_exam %>
    <%= form.text_field :mid_exam %>
  </div>

  <div class="field">
    <%= form.label :final_exam %>
    <%= form.text_field :final_exam %>
  </div>

  <div class="field">
  <%= form.hidden_field :course_id, :value => params[:course_id] if params[:course_id] %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
